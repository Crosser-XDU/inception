model_name_or_path: meta-llama/Meta-Llama-3-8B-Instruct
trust_remote_code: true
stage: sft
do_train: true
finetuning_type: lora
lora_target: all
use_recurft: true
recurft_loop_start_layer: 29
recurft_loop_end_layer: 30
recurft_pre_lora_rank: 8
recurft_last_lora_rank: 8
recurft_t_lora_rank: 128
recurft_hidden_loss_weight: 20.0
recurft_hidden_relative_mse_loss_weight: 1.0
recurft_hidden_cosine_loss_weight: 1.0
recurft_recurrent_loss_weight: 1.0
recurft_recurrent_relative_mse_loss_weight: 0.05
recurft_recurrent_cosine_loss_weight: 0.05
recurft_recurrent_warmup_steps: 200
recurft_multistep_loss_weight: 0.0
recurft_kl_loss_weight: 0.2
recurft_kl_temperature: 1.0
recurft_sft_loss_weight: 0.0
recurft_loss_on_labels_only: false
recurft_verifier_lora_rank: 32
dataset: metamathqa_full_sft
dataset_dir: experiments/recurft_math/full_data
template: llama3
cutoff_len: 2048
overwrite_cache: false
preprocessing_num_workers: 8
output_dir: outputs/llama3_8b_tail29_30_r8_8_128_20260615
overwrite_output_dir: true
logging_steps: 100
save_steps: 100
save_total_limit: 2
save_only_model: true
plot_loss: true
report_to: wandb
run_name: llama3_8b_tail29_30_r8_8_128_20260615
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 1.0e-5
lr_scheduler_type: constant
warmup_ratio: 0.0
weight_decay: 0.0
num_train_epochs: 1.0
seed: 42
max_grad_norm: 1.0
bf16: true
flash_attn: auto
disable_gradient_checkpointing: false
ddp_timeout: 180000000
